# V458 — Production Report Preparation Target display

- Fixed Masking/Unmasking Preparation Target column.
- Removed support-step numbering from Target (for example `1. 13:00`).
- Target now uses the linked Main schedule window and renders consistently with normal Production rows: `DD/MM HH:mm` then `→ DD/MM HH:mm`.
- Unmasking → Masking ordering remains only in Preparation Operations.
- No change to Masking/Unmasking strict configuration, execution state, Planning Chain, Batch, Scheduling, or Auto Planning.
