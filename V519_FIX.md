# V519 · Masking Time smaller than Batch No.

- Changed only the scheduled Batch-table Masking Time font size.
- `Masking time HH:MM` is now 8px, while the table/Batch number base is 9px.
- One-line compact presentation is unchanged.
- No Masking calculation, READY/WAIT, Batch, Scheduling, Production, Chat or realtime logic changed.
- No SQL migration required.
