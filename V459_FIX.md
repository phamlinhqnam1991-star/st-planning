# V459 FIX — Compact Flybar link marker

- Chemical Line linked-row marker is now a compact `×` only.
- Removed the `Dòng ...` text and arrow from the row marker.
- Link-pair colors and 1-to-1 Flybar/Precleaning logic are unchanged.
- Tooltip still exposes the linked source for traceability.
- Row height now stays aligned with normal Flybar rows.
