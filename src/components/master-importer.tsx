"use client";import { useState } from "react";import { createClient } from "@/lib/supabase/client";export function MasterImporter(){const[file,setFile]=useState<File|null>(null),[status,setStatus]=useState(""),[busy,setBusy]=useState(false),[resetBusy,setResetBusy]=useState(false);async function run(){if(!file)return;setBusy(true);setStatus("Đang upload...");try{const s=createClient(),safe=file.name.replace(/[^a-zA-Z0-9._-]/g,"_"),path=`${new Date().toISOString().replace(/[:.]/g,"-")}_${safe}`;const{error}=await s.storage.from("master-imports").upload(path,file,{upsert:false});if(error)throw error;setStatus("Đang so sánh NEW / CHANGED...");const r=await fetch("/api/import/master",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({path,fileName:file.name})}),d=await r.json();if(!r.ok)throw new Error(d.error||"Import failed");setStatus(`Hoàn tất: ${d.sourceRows.toLocaleString()} dòng · Mới ${d.newRows.toLocaleString()} · Thay đổi ${d.changedRows.toLocaleString()} · Không đổi ${d.unchangedRows.toLocaleString()}.`);setTimeout(()=>location.reload(),1800)}catch(e){setStatus(`Lỗi: ${e instanceof Error?e.message:String(e)}`)}finally{setBusy(false)}}async function resetAll(){if(!confirm("Reset toàn bộ Master Data và Import History? ST Operation Scope hệ thống sẽ được giữ lại."))return;if(prompt('Nhập chính xác RESET để xác nhận:')!=="RESET")return;setResetBusy(true);try{const r=await fetch("/api/master/reset",{method:"POST"}),d=await r.json();if(!r.ok)throw new Error(d.error||"Reset failed");setStatus("Reset hoàn tất.");setTimeout(()=>location.reload(),1000)}catch(e){setStatus(`Lỗi: ${e instanceof Error?e.message:String(e)}`)}finally{setResetBusy(false)}}return <div className="card"><h2 style={{marginTop:0}}>Import Master Excel</h2><p className="muted">Lần đầu Full Import. Từ lần 2 chỉ NEW/CHANGED được cập nhật; UNCHANGED bỏ qua. Dữ liệu biến mất chuyển inactive.</p><div className="row"><input className="input" type="file" accept=".xlsx" onChange={e=>setFile(e.target.files?.[0]||null)}/><button className="btn primary" disabled={!file||busy||resetBusy} onClick={run}>{busy?"Đang xử lý...":"Import Master"}</button><button className="btn danger-btn" disabled={busy||resetBusy} onClick={resetAll}>{resetBusy?"Đang reset...":"Reset All Master Data"}</button></div>{status&&<p className={status.startsWith("Lỗi")?"danger":"muted"}>{status}</p>}</div>}