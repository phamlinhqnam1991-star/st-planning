import {redirect} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {createAdminClient} from "@/lib/supabase/admin";import {StGroupManager} from "@/components/st-group-manager";import {AppTabs,SubTabs} from "@/components/app-tabs";
export const dynamic="force-dynamic";
const tabs=[{key:"operation",label:"Operation Master",href:"/master/operation"},{key:"operationmapping",label:"ST Operation Mapping",href:"/master/operationmapping"},{key:"stgroup",label:"ST Group Master",href:"/st-groups"},{key:"area",label:"Area Master",href:"/area"}];
export default async function Page(){const auth=await createClient();const {data:{user}}=await auth.auth.getUser();if(!user)redirect("/login");const admin=createAdminClient();const {data,error}=await admin.from("md_st_group").select("*").eq("is_active",true).order("sort_order");if(error)throw error;return <main className="shell"><div className="top"><div className="brand"><h1>ST Group Master</h1><p>{data?.length||0} active groups · Add / Edit / Deactivate · {user.email}</p></div></div><AppTabs active="config"/><SubTabs items={tabs} active="stgroup"/><div className="section"><StGroupManager rows={(data||[]) as any}/></div></main>}
